type: object
properties:
  apiVersion:
    type: string
  kind:
    type: string
  meta:
    type: object
    properties:
      name:
        type: string
    required: [name]
  spec:
    type: object
required: [apiVersion, kind, meta, spec]
